# GitHub 保卫战 · 最终评分

> 评分基准：阅读仓库最终代码状态、所有已关闭 Issue、所有 PR（含已关闭）及 GitHub Actions CI 结果。  
> 分值构成：D1（现代 C++ 语法，25 分）+ D2（修复过程，40 分）+ D3（Issue 准确性与修复完整性，35 分）− SHM_white 介入扣分。

---

## 洪嘉源（GitHub：2w84u）

### 1. 现代 C++ 语法（D1）

**得分：23 / 25**

| 文件 | 评价 |
|------|------|
| q01 | `auto pi`、`decltype(pi) e`、尾置返回类型、range-for + auto 均正确；新增 `product_vector` 功能干净 |
| q02 | 捕获方式合理（`[n]` 精确捕获代替 `[=]` 可接受）；mutable 语义理解正确 |
| q03 | make_unique / make_shared / weak_ptr 用法全部正确 |
| q04 | `std::move` + `std::forward` 用法正确 |
| q05～q06 | 结构化绑定、optional 均正确 |
| q07 | `constexpr long long`、`std::is_integral_v<T>`、`consteval` 均用法标准 |
| q08 | concept / requires / 缩写模板均正确且简洁 |
| q09 | `override`、`final`、`=default`、`=delete` 完整使用；委托构造中 `Config(h, p)` 未对 `h` 使用 `std::move`，存在不必要的拷贝（−1 分）；`Config()` 委托调用 `Config(std::string("localhost"), 8080)` 正确 |
| q10 | ranges + views 管道全部正确且惯用 |

### 2. 修复过程（D2）

**得分：35 / 40**

- **Issue #13** 内容质量极高：列出了全部 11 处破坏点，每处均标明了文件路径、行号、错误原因及发现方法（通过 `CHECK_EQ` 宏输出比对、调用堆栈等），逻辑清晰，没有错误描述。
- **PR #14** 单次提交即被合并，改动精确，只修改了被破坏的代码行，同时附加了 `gitLearningReport.md` 学习报告，无多余变更引入。
- 整个流程"Issue → PR → Merge"一气呵成，无失败 PR，无需管理员介入。
- 扣分原因：PR 描述中无 Issue 编号引用（−3 分）；新功能（`product_vector`）没有单独 PR 说明，但已保留（−2 分）。

### 3. Issue 准确性与修复完整性（D3）

**得分：34 / 35**

- Issue #13 覆盖了全部 11 处破坏：`lib/test_utils` 头文件路径、q01 `-=`、q02 `-`、q03 `std::move` 语义误用、q04 缺 `std::move`、q05 `split_pair` 返回未交换、q06 判断条件取反、q07 `0LL`、q08 `+` 代替 `*`、q09 `"Bark"` 错误返回值、q10 奇偶过滤条件取反，识别率 **100%**。
- CI 最终结果：**10/10 测试通过**（100% pass rate）。
- 新增 `product_vector` 功能已正确保留并通过测试（−1 分：q10 中 `count_above_threshold` 新增功能未在洪嘉源自己的 q10 中体现，略有遗漏）。

### SHM_white 介入扣分

**扣分：0 分**

无管理员提醒或干预记录。

---

**洪嘉源总分：23 + 35 + 34 − 0 = 92 / 100**

---

---

## 武文竣（GitHub：wwj-nuaa）

### 1. 现代 C++ 语法（D1）

**得分：21 / 25**

| 文件 | 评价 |
|------|------|
| q01 | `auto`、`decltype`、range-for 用法正确；遗留注释 `// 新添加的功能测试代码` 中 `param_version` 使用 `double` 代替 `auto`，轻微风格问题 |
| q02 | Lambda 写法完整，捕获方式正确 |
| q03 | 智能指针用法正确，`reset()` 语义理解到位 |
| q04 | 移动语义写法正确；但返回语句末尾遗留 `/* _____ */;` 形成双分号（`return std::move(src);/* _____ */;`），虽然实际只有一个 `return`，但属于代码残留（−0.5 分） |
| q05～q06 | 正确 |
| q07 | `constexpr long long` + `1LL` 标准，`std::is_same_v` chain 同贡丞叶；此处写法惯用（只需通过测试）——实际实现正确 |
| q08 | 正确 |
| q09 | **q09 存在问题**：Dog::sound() 修复了函数逻辑，但 `override` 关键字仍然缺失（保留了破坏遗留的 `/* _____ */` 占位符）；Cat::sound() 在 PR #20 中被**主动移除了 `override`**，引入了回归错误（−3.5 分）。两者均应有 `override`，均不合格。 |
| q10 | ranges 管道实现正确，新增 `count_above_threshold`（以 `constexpr int COUNT_THRESHOLD = 3` 为阈值）实现正确且简洁 |

### 2. 修复过程（D2）

**得分：29 / 40**

- **Issue #12** 内容详细，附上了编译/测试日志（`default.txt`），识别出了全部 10 处破坏点，质量较高。
- **PR #15**（已关闭）：第一次 PR 极度混乱——不仅包含武文竣自己的修复，还将整个 GitHub Actions 工作流文件、以及其他三位同学的文件夹全部一次性提交，造成超大 PR（207KB diff），不符合"小步提交"的工程实践（−7 分）。
- **PR #20**：第二次提交干净许多，精确修复了 7 处破坏，格式规范，合并成功。
- 但 PR #20 中 q09 存在回归错误（对 Cat::sound() 的 `override` 不恰当地移除），说明修改时不够仔细（−4 分）。

### 3. Issue 准确性与修复完整性（D3）

**得分：33 / 35**

- Issue #12 覆盖了全部 10 处武文竣代码的破坏点，识别率 **100%**。
- CI 最终结果：**10/10 测试通过**（功能测试全部通过）。
- 但 q09 中 `override` 问题（Dog 未恢复、Cat 新引入缺失）属于非功能性回归，不影响运行时测试但降低了代码质量（−2 分）。

### SHM_white 介入扣分

**扣分：0 分**

无管理员提醒或直接干预记录。

---

**武文竣总分：21 + 29 + 33 − 0 = 83 / 100**

---

---

## 贡丞叶（GitHub：gcy3answer）

### 1. 现代 C++ 语法（D1）

**得分：17 / 25**

| 文件 | 评价 |
|------|------|
| q01 | **致命错误**：`sum *= x`（应为 `sum += x`），这是被破坏的代码，**贡丞叶未能识别并修复**，导致该测试用例运行时返回 0 而非 15，q01 测试**失败**（−5 分）；`pi`、`decltype(pi) e`、尾置返回类型均已正确修复；代码中保留了 `// 破坏：改为显式的 int` 注释（−0.5 分） |
| q02 | make_counter 中捕获方式已正确修复回 `[=]` mutable |
| q03～q06 | 已正确 |
| q07 | `constexpr long long` + `1LL` 标准；`type_category<T>` 使用 `std::is_same_v<T, int>` 和 `std::is_same_v<T, char>` 两次判断，而非惯用的 `std::is_integral_v<T>`，语义范围较窄（−1 分）；`static_assert(MAX_SIZE > 0)` 无信息字符串（可接受）；`consteval` 正确 |
| q08～q09 | 已正确修复 |
| q10 | 无 q10（该题在破坏时被 CMakeLists 屏蔽，CMakeLists 已修复，但代码目录未添加，暂无法评估） |

### 2. 修复过程（D2）

**得分：20 / 40**

- **Issue #7** 识别出了 7 处破坏（q01 `e` 值变更和多余 `#include`、q02 `[&]` 捕获、q03 `&p`、q05 `+z`、q07 CMakeLists 多余参数 `111`、q09 返回值和 `false` 错误），但**遗漏了** q01 的 `int pi=3.14`（已在 PR 中修复但未在 Issue 中提及）和最关键的 **`sum *= x`**（既未在 Issue 中提及，也未在 PR 中修复）（−5 分）。
- **PR #16**（已关闭）：疑似存在合并冲突，关闭重试。
- **PR #17**（已关闭）：CI 不稳定，关闭。
- **PR #18**（已关闭）：同上，关闭。
- **PR #19**（已合并）：第四次 PR 才成功合并，流程极不流畅（−10 分）。PR 内容"修复 q07"，实际修复了多个问题，但标题与实际内容不符，描述不清晰（−2 分）。

### 3. Issue 准确性与修复完整性（D3）

**得分：22 / 35**

- Issue #7 覆盖了约 7/10 处破坏，漏掉了 `int pi`（修复了但未提）和 `sum *= x`（既未提及也未修复），Issue 识别率约 **70%**（−5 分）。
- CI 最终结果：**9/10 测试通过**（q01 `sum_vector` 测试失败，期望 15，实际为 0）（−6 分）。
- CMakeLists 中 `q01_auto_type_deduction_missing` 重命名和 q08 屏蔽均已修复（在 PR #19 中），但未在 Issue 中提及（−2 分）。

### SHM_white 介入扣分

**扣分：−2 分**

Issue #7 评论中，SHM_white 提醒"请开一个 pull request 进行修复"（1 次提醒）。

---

**贡丞叶总分：17 + 20 + 22 − 2 = 57 / 100**

---

---

## 杨洵（GitHub：1546742976）

### 1. 现代 C++ 语法（D1）

**得分：20 / 25**

| 文件 | 评价 |
|------|------|
| q01 | `auto pi`、`decltype`、尾置返回类型均正确；range-for 修复正确 |
| q02 | Lambda 写法均正确，mutable 语义理解正确 |
| q03 | 智能指针用法正确 |
| q04 | 移动语义写法正确 |
| q05～q06 | 正确 |
| q07 | **类型精度问题**：`constexpr int factorial(int n)` 使用 `int` 而非 `long long`（题目要求为 `long long`），使用 `1LL` 字面量但函数返回类型是 `int`，类型不一致（虽然测试用值 ≤10! 不溢出，但不符合题目规范）（−2 分）；`if constexpr (std::is_integral_v<T>)` 用法正确 |
| q08 | 正确 |
| q09 | `override`、`final`、`=default`、`=delete` 均已正确使用；委托构造正确（−3 分原因：`Config(std::string h, int p) : Config(std::move(h), p, false)` 已正确使用 `std::move`） |
| q10 | ranges 管道实现基本正确；`COUNT_THRESHOLD = 5`（原始代码值），CI 测试中期望值为 2（`{1,5,3,8,2,7}` 中 >5 的有 2 个），与测试一致 |

### 2. 修复过程（D2）

**得分：18 / 40**

- **Issue #8** 内容有一定覆盖，但部分描述不准确：
  - "q03 缺少头文件 #include<memory>"——实际破坏是修改了测试断言值（`use_count()` 期望从 2 改为 3），并非缺少头文件（−2 分）
  - "q04 缺少 #include<string> 和 #include<utility>"——实际是缺少这两个头文件（描述方向正确但细节混淆），会导致编译失败（可接受）
  - 整体描述较粗糙，但覆盖了主要破坏点
- **PR #9**（已关闭）：**将整个文件夹从 `杨洵/` 重命名为 `【修复】 杨洵 破坏修复/`**，破坏了仓库结构，属于严重错误做法（−8 分）。
- **PR #11**（已关闭）：**再次重命名文件夹**（仍错误），同时附加了 `git学习报告.md`，但文件夹结构问题未解决（−5 分）。
- 两个 PR 均未被合并，最终由 SHM_white 直接合并分支（参见下方扣分）。

### 3. Issue 准确性与修复完整性（D3）

**得分：30 / 35**

- Issue #8 覆盖了约 8/10 处破坏，部分描述有误差（−4 分）。
- CI 最终结果：**10/10 测试通过**（合并后全部通过）（满分）。
- 代码修复本身质量良好，实际 bug 均已修复，q07 `MAX_SIZE` 从 100 恢复为 256、sort 方向修复等均正确（−1 分：q10 新增功能 `COUNT_THRESHOLD = 5` 与当前测试期望值不完全一致，存在潜在问题）。

### SHM_white 介入扣分

**扣分：−8 分**

SHM_white 在 Issue #8 评论"已完成修复和合并"后，直接合并了 `1546742976-main` 分支（commit `3747de9 Merge branch '1546742976-main'`），并通过 commit `d78e028 更改结构` 修正了文件夹命名问题。这超出了"提醒"范畴，属于实质性干预。

---

**杨洵总分：20 + 18 + 30 − 8 = 60 / 100**

---

---

## 总分汇总

| 排名 | 姓名 | D1 语法（25） | D2 过程（40） | D3 修复（35） | SHM 扣分 | **总分** | 等级 |
|------|------|-------------|-------------|-------------|--------|---------|------|
| 🥇 1 | 洪嘉源 | 23 | 35 | 34 | 0 | **92** | 优秀 |
| 🥈 2 | 武文竣 | 21 | 29 | 33 | 0 | **83** | 良好 |
| 🥉 3 | 杨洵   | 20 | 18 | 30 | −8 | **60** | 及格 |
| 4 | 贡丞叶 | 17 | 20 | 22 | −2 | **57** | 及格 |

---

## 综合评价

### 洪嘉源 · 92 分
本次表现最为突出。Issue #13 是四人中最详尽的一份，精确到每个文件的行号和列号，并说明了每处破坏的发现方式。PR #14 一次性修复了全部 11 处破坏，代码质量高，流程规范。唯一扣分点在于委托构造函数中对 `std::move` 的使用欠缺（不影响功能但有拷贝开销），以及 PR 中未关联 Issue 编号。

### 武文竣 · 83 分
Issue #12 内容详尽且附有日志文件，问题定位能力较强。但第一次 PR #15 将所有人的代码和 CI 工作流全部打包到一个超大 PR 中，属于工程实践的严重问题，最终不得不关闭重做。PR #20 质量合格，但修复 q09 时不仅未能恢复 Dog 的 `override`，还额外移除了 Cat 的 `override`，说明在局部修改时不够谨慎。

### 杨洵 · 60 分
代码修复本身基本到位（测试全部通过），Issue 也覆盖了主要破坏点，但 PR 流程存在根本性错误——连续两次 PR 均重命名了整个文件夹，不仅不符合 Git 工程规范，也导致两次 PR 都无法被合并，最终需要 SHM_white 出手。q07 中 `factorial` 使用 `int` 返回类型而非 `long long` 也属于现代 C++ 规范意识不足的体现。

### 贡丞叶 · 57 分
最主要的问题是 **q01 的 `sum *= x`（应为 `sum += x`）始终未被识别和修复**——这是破坏引入的一处明显错误（`sum_vector({1,2,3,4,5})` 期望 15 实际返回 0），导致最终仍有测试失败。Issue 遗漏了该项，PR 也未修复。四次 PR 尝试才成功合并，流程最为混乱。q07 `type_category` 使用 `std::is_same_v<T, int>` 手动枚举而非 `std::is_integral_v<T>`，反映出对标准库 type traits 的掌握不够熟练。
