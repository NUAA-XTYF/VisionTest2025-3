# GitHub 保卫战 · 评分标准

> 本次考核模拟了"自己的代码被人改坏"的场景，考查四人在 **现代 C++ 语法掌握程度**、**修复过程合理性与熟练度**、**问题定位的准确性与最终修复效果** 三个维度上的综合表现。

---

## 一、总分构成（满分 100 分）

| 维度 | 分值 | 权重 | 说明 |
|------|------|------|------|
| D1：现代 C++ 语法掌握程度 | 25 分 | 25% | 以仓库最终状态为准，阅读各自文件夹下的代码进行评分 |
| D2：修复过程合理性与熟练度 | 40 分 | 40% | 评估 Issue 质量、PR 质量、提交流程规范性 |
| D3：Issue 描述准确性与修复完整性 | 35 分 | 35% | Issue 内容是否与实际破坏匹配，最终代码是否恢复到破坏前 |

> **附加扣分项：SHM_white 介入程度**  
> 若 SHM_white 进行了超出必要提醒范围的干预（如直接代劳合并、替学生修改结构等），对应学生酌情扣分（上限 −10 分）。

---

## 二、维度 D1：现代 C++ 语法掌握程度（25 分）

评估各题答案是否使用了正确、惯用的现代 C++ 写法，以最终 `main` 分支上各人文件夹内的代码为准。

### 2.1 评分要点

| 题目 | 考查知识点 | 评分关注点 |
|------|-----------|-----------|
| q01 | `auto`、`decltype`、尾置返回类型、range-for | `auto` / `decltype` 用法是否准确；迭代器写法是否惯用；range-for 累加逻辑是否正确 |
| q02 | Lambda：值捕获 `[=]`/`[n]`、引用捕获 `[&]`、泛型 lambda、`mutable` | 捕获方式是否匹配语义；`mutable` 使用是否正确；返回类型是否推导正确 |
| q03 | `unique_ptr`、`shared_ptr`、`weak_ptr`、`make_unique/shared` | 所有权转移（`std::move`）是否正确；引用计数语义是否理解 |
| q04 | `std::move`、右值引用 `&&`、移动构造、`std::forward` | 是否实际触发移动语义；完美转发是否使用 `std::forward` |
| q05 | 结构化绑定 | 绑定写法是否正确；返回值逻辑是否符合题意 |
| q06 | `std::optional`、`std::variant` | 返回 `nullopt` 的条件是否正确 |
| q07 | `constexpr`、`if constexpr`、`static_assert`、`consteval` | 函数签名类型精度（`long long` vs `int`）；`std::is_integral_v<T>` vs 枚举判断 |
| q08 | `concept`、`requires`、缩写函数模板 | 概念定义、约束应用、范围求和是否正确 |
| q09 | `override`、`final`、`=default`、`=delete`、委托构造 | `override` 是否完整标注；委托构造是否合理（`std::move` 优化） |
| q10 | `std::ranges`、`std::views`、管道操作符 `\|` | Range 适配器链式调用是否惯用；新增功能实现质量 |

### 2.2 扣分原则

- **功能性错误**（导致测试失败）：每处扣 3～5 分，视影响范围而定  
- **类型精度问题**（如 `int` 代替 `long long`）：扣 1～2 分  
- **非惯用写法**（如手动枚举替代标准 trait，可以接受但不推荐）：扣 1 分  
- **遗留破坏标记注释**（如 `// 破坏：…` 未清除）：扣 0.5 分  
- **代码风格问题**（不必要的占位符 `/* _____ */` 残留等）：扣 0.5 分

---

## 三、维度 D2：修复过程合理性与熟练度（40 分）

### 3.1 Issue 质量（12 分）

| 评分项 | 满分 | 说明 |
|--------|------|------|
| 描述完整性 | 5 分 | 是否列出所有被破坏的文件、位置与原因 |
| 描述准确性 | 4 分 | 描述是否与实际破坏一致，有无张冠李戴或错误归因 |
| 定位方法说明 | 3 分 | 是否说明了如何发现（运行测试、git diff、阅读代码等） |

### 3.2 PR 质量与流程规范（28 分）

| 评分项 | 满分 | 说明 |
|--------|------|------|
| PR 结构合理性 | 10 分 | 是否在正确的文件夹/分支上修改；是否只改应改的内容，不引入无关变更 |
| PR 描述与关联 Issue | 5 分 | PR 标题/描述是否清晰；是否关联了对应 Issue |
| 一次成功率 | 8 分 | 第一次 PR 是否直接被合并；多次失败 PR 酌情扣分 |
| 修复精准度 | 5 分 | 修复是否精确只改动了被破坏的内容，有无意外引入新错误 |

---

## 四、维度 D3：Issue 描述准确性与修复完整性（35 分）

### 4.1 Issue 与实际破坏的匹配程度（15 分）

按以下方式给分：
- 每个实际被破坏的点（共约 10 个/人）对应约 1.5 分
- Issue 中正确覆盖该破坏点的，得满分
- 未提及、描述错误或与实际破坏不符的，扣除对应分值

### 4.2 最终修复效果（20 分）

| 评分项 | 满分 | 说明 |
|--------|------|------|
| 单元测试通过率 | 12 分 | 全部 10 个测试通过得满分；每失败 1 个扣 2 分（参考 CI 最终结果） |
| 新增功能是否保留 | 4 分 | 在修复破坏的同时，是否正确保留了 CI 新增的功能代码及其测试 |
| 修复是否引入新错误 | 4 分 | 修复过程中有无意外删除/修改了正确的代码（如误删 `override`、误改逻辑等） |

---

## 五、SHM_white 介入扣分说明

- **轻微提醒**（如 Issue 评论"请开一个 PR 进行修复"）：扣 2 分  
- **中度介入**（如直接代劳合并 PR、帮忙修改文件夹结构等）：扣 5～10 分  
- **完全代劳**（直接替学生修复代码并提交）：酌情扣至 0 分

---

## 六、评分参考表（综合）

| 等级 | 分值范围 | 描述 |
|------|---------|------|
| 优秀 | 85 ～ 100 | Issue 准确全面，一次 PR 修复到位，代码质量高，所有测试通过 |
| 良好 | 70 ～ 84 | Issue 基本准确，PR 流程有小瑕疵，代码存在轻微问题，测试全部或基本通过 |
| 及格 | 55 ～ 69 | Issue 漏项或不准确，PR 需多次尝试或需管理员干预，存在未修复的破坏点 |
| 不及格 | < 55 | Issue 严重缺失，修复流程混乱，存在较多未修复的破坏点，测试失败数量多 |
